# Project identity (single source of truth: pyproject.toml [project].name)
PROJECT_NAME=groundtruth

# Postgres + pgvector + pgvectorscale
DATABASE_URL=postgresql://groundtruth:groundtruth@localhost:5432/groundtruth

# Anthropic — for Agent SDK + DeepEval LLM-judged metrics
ANTHROPIC_API_KEY=

# Voyage AI — embeddings (voyage-4-large default)
VOYAGE_API_KEY=
